mod print;
mod vars;
mod types;

// To run the code put the following in the terminal
// cargo run
// For a release build do
// cargo build --release
fn main() {
    // println!("Hello, world!");
    // print::run();
    // vars::run();
    types::run();
}
